# ============================================================
# RAGpy - Environment Variables Template
# ============================================================
# Copy this file to .env and fill in your API keys
# All variables are optional except OPENAI_API_KEY

# ------------------------------------------------------------
# OpenAI (REQUIRED for embeddings)
# ------------------------------------------------------------
OPENAI_API_KEY=sk-proj-...

# ------------------------------------------------------------
# OpenRouter (OPTIONAL - for cost-effective LLM text recoding)
# ------------------------------------------------------------
# If configured, you can use OpenRouter models (e.g., Gemini 2.5 Flash)
# for text recoding instead of OpenAI GPT-4o-mini, reducing costs by 2-3x.
# Format: provider/model (e.g., "openai/gemini-2.5-flash", "anthropic/claude-3-5-sonnet")
# Get your API key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=
OPENROUTER_DEFAULT_MODEL=google/gemini-2.5-flash

# ------------------------------------------------------------
# Mistral (OPTIONAL - for PDF OCR extraction)
# ------------------------------------------------------------
# Used by rad_dataframe.py for high-quality OCR extraction.
# Get your API key at: https://console.mistral.ai/api-keys/
MISTRAL_API_KEY=
MISTRAL_OCR_MODEL=mistral-ocr-latest
MISTRAL_API_BASE_URL=https://api.mistral.ai

# ------------------------------------------------------------
# Pinecone (OPTIONAL - vector database)
# ------------------------------------------------------------
PINECONE_API_KEY=pcsk_...
PINECONE_ENV=https://your-index-name-xxxxx.svc.aped-xxxx-xxxx.pinecone.io

# ------------------------------------------------------------
# Weaviate (OPTIONAL - vector database)
# ------------------------------------------------------------
WEAVIATE_API_KEY=...
WEAVIATE_URL=https://xxxxx.c0.europe-west3.gcp.weaviate.cloud

# ------------------------------------------------------------
# Qdrant (OPTIONAL - vector database)
# ------------------------------------------------------------
QDRANT_API_KEY=...
QDRANT_URL=https://xxxxx.eu-central-1-0.aws.cloud.qdrant.io

# ------------------------------------------------------------
# Zotero (OPTIONAL - for automatic reading notes generation)
# ------------------------------------------------------------
# Generate reading notes automatically and push them to Zotero as child notes.
# Get your API key at: https://www.zotero.org/settings/keys/new
# Required permissions: "Allow library access" + "Allow notes access"
# User ID and Group ID are usually auto-detected from Zotero export.
ZOTERO_API_KEY=
ZOTERO_USER_ID=
ZOTERO_GROUP_ID=

# ------------------------------------------------------------
# Resend (OPTIONAL - for email verification and password reset)
# ------------------------------------------------------------
# Used for sending verification emails and password reset links.
# Get your API key at: https://resend.com/api-keys
# You need a verified domain, or use the test domain (onboarding@resend.dev)
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=noreply@yourdomain.com

# ------------------------------------------------------------
# Application Settings
# ------------------------------------------------------------
# Base URL for email links (verification, password reset)
APP_URL=http://localhost:8000

# ============================================================
# Notes:
# - OPENAI_API_KEY is mandatory (used for embeddings generation)
# - OpenRouter credentials are optional (fallback to OpenAI if not set)
# - At least ONE vector database (Pinecone/Weaviate/Qdrant) should be configured
# - Zotero credentials are optional (only needed for automatic note generation)
# - You can configure credentials via the web UI (Settings button)
# ============================================================
